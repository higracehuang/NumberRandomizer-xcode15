import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
          Text("?")
          Button {
            
          } label: {
            Text("Generate Random Number")
          }
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
